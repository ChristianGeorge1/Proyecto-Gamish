<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">

    <link rel="stylesheet" href="../public/styles.css">

    <title>Juegos</title>
</head>
<body>
    <h1 style="text-align: center;">Juegos</h1>

    <div class="container">
        <% juegos.some(function(o){  %>
        <div class="row">

            <div class="col s12 m6">

                <div class="card light-blue darken-4">
                    <div class="card-content white-text">
                        <span class="card-title"><%= o.nombre %></span>
                        <p><strong class="green-text text-ligthen-4">Desarrolladora: </strong><%= o.compañia %></p>
                        <p><%= o.descripcion %></p>
                        <p><strong class="green-text text-ligthen-4">Categoría: </strong></strong><%= o.categoria %></p>
                        <p><strong class="green-text text-ligthen-4">Plataformas: </strong><%= o.plataformas %></p>
                        <p><strong class="green-text text-ligthen-4">Precio: </strong>$<%= o.costo %>.00</p>

                    </div>
                    <div class="card-action">
                        <form>
                            <button type="submit" class="btn waves-effect green accent-4">Comprar
                                <i class="material-icons right">shopping_cart</i>
                            </button>
                        </form>
                     
                    </div>
                </div>
            </div>
        <% }); %>
        </div>  
        
    </div>
    
    <ul>
        <% juegos.forEach(function(o){ %>
            <li><%= o.nombre %> --- <%= o.costo %> --- <%= o.compañia %> --plataformas:  
            <%= o.plataformas %>
            <%= o.descripcion %>
            <%= o.ruta %>  
        </li>  
        <% }); %> 
    </ul>


    <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
</body>
</html>